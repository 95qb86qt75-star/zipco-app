# Copy to .env.development.local and replace the placeholder locally.
# Never reuse production, database, JWT, Railway, or ConnectUS credentials.
VITE_API_URL=http://127.0.0.1:3000
VITE_ENABLE_DEV_AUTH=true
VITE_DEV_AUTH_KEY=replace-with-a-local-only-key
