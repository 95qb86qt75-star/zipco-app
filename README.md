
  # Local Discovery App Design

  This is a code bundle for Local Discovery App Design. The original project is available at https://www.figma.com/design/cOb9iJfA4MQR28aRLclUUu/Local-Discovery-App-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  